<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Khattabook</title>
    <link rel="stylesheet" href="/public/css/style.css" />
    <link
      href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css"
      rel="stylesheet"
    />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body>
    <main class="w-full h-screen bg-zinc-100 font-['Helvetica']">
      <nav class="flex px-10 justify-between items-center py-5 border-b">
        <a class="tracking-tight" href="/">
          <h3
            class="text-2xl tracking-tight italic px-2 py-1 rounded border border-emerald-700"
          >
            Khattabook
          </h3></a
        >
        <div class="navlinks flex gap-5 items-center justify-between">
          <a
            class="tracking-tight px-4 py-2 bg-emerald-700 text-white rounded-md hover:bg-emerald-600 cursor-pointer transition-all duration-300"
            href="/create"
            >Create New Hisaab</a
          >
        </div>
      </nav>
      <div class="px-10 hisaabs">
        <h3 class="capitalize text-2xl font-medium mb-5 mt-10 tracking-tight">
          All hisaab kitaab
        </h3>

        <% if (!files || files?.length === 0) { %>
        <h3 class="text-zinc-500">create hisaabs to see something.</h3>
        <% } else { %> <% files.forEach((file) => { %>
        <div
          class="hisaab w-fit min-w-[300px] items-center py-3 mb-3 px-5 gap-20 bg-emerald-700 flex justify-between rounded-md"
        >
          <div class="flex gap-4 text-white items-center">
            <h3 class="text-2xl"><%= file.split(".")[0] %></h3>
            <a href="/hisaab/<%= file %>"
              ><i
                class="w-6 h-6 text-emerald-600 flex items-center justify-center bg-white rounded-full ri-arrow-right-line hover:bg-gray-700 hover:text-white transition-all duration-300"
              ></i
            ></a>
          </div>
          <div class="flex gap-3 items-center">
            <a href="/edit/<%= file %>"
              ><i
                class="w-6 h-6 text-white flex items-center justify-center ri-pencil-line rounded-full transform hover:scale-125 transition-all duration-300 cursor-pointer"
              ></i>
            </a>
            <a href="/delete/<%= file %>"
              ><i
                class="w-6 h-6 text-red-500 hover:bg-gray-700 hover:text-white transition-all duration-300 cursor-pointer flex items-center justify-center bg-white rounded-full ri-delete-bin-line"
              ></i
            ></a>
          </div>
        </div>
        <% }); %> <% } %>
      </div>
    </main>
  </body>
</html>
